[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "gunicorn wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 2"

[service]
name = "guanghe-bottle"
